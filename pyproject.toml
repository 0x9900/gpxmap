[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gpxmap"
dynamic = ["version"]
description = "Use WSPR data to analyze your antenna's performance"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
    { name = "Fred C.", email = "github-fred@hidzz.com" }
]
keywords = ["ham radio", "wspr", "antenna"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: Telecommunications Industry",
    "License :: OSI Approved :: BSD License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "folium",
    "gpxpy",
    "matplotlib",
    "numpy",
    "pandas",
    "tzlocal",
    "pyproj",
    "simplekml",
]

[project.urls]
Homepage = "https://github.com/0x9900/gpxmap"
Repository = "https://github.com/0x9900/gpxmap"

[project.scripts]
gpxmap = "gpxmap.gpxmap:main"

[tool.hatch.build.targets.wheel]
packages = ["src/gpxmap"]

[tool.hatch.version]
path = "src/gpxmap/__init__.py"

[project.optional-dependencies]
dev = [
    "build",
    "flake8",
    "geo_ham",
    "isort",
    "mypy",
    "pre-commit",
    "pylint",
    "twine",
]

[tool.mypy]
ignore_missing_imports = true
